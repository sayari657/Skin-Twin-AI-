# Django et extensions
Django==5.2.6
djangorestframework==3.15.2
django-cors-headers==4.3.1
djangorestframework-simplejwt==5.3.0

# Base de données SQLite (pas besoin de mysqlclient pour Docker)
# mysqlclient==2.2.4  # Commenté - utilise SQLite par défaut
# PyMySQL==1.1.0      # Commenté - utilise SQLite par défaut

# Machine Learning (essentiel pour le projet)
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0
# Optionnel - peut être commenté pour build plus rapide
# torch>=2.0.0
# torchvision>=0.15.0
# ultralytics>=8.0.0

# GAN et modèles avancés (optionnel - peut être commenté pour build plus rapide)
# tensorflow>=2.15.0
# transformers>=4.35.0

# Utilitaires
python-decouple==3.8
requests>=2.31.0
beautifulsoup4>=4.12.0
# celery==5.3.4      # Commenté si pas utilisé
# redis==5.0.1       # Commenté si pas utilisé

# Développement
# django-debug-toolbar==4.2.0  # Commenté pour production
# pytest==7.4.3
# pytest-django==4.7.0

# Production
gunicorn==21.2.0
whitenoise==6.6.0

